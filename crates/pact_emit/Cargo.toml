[package]
name = "pact_emit"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Pact to TypeScript transpiler with source maps. Must not re-implement checking."

[dependencies]
pact_check = { path = "../pact_check" }
