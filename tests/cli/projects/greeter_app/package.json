{
  "name": "kei-cli-project-greeter-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "kei build の CLI 統合フィクスチャ(M36)。extern package 束縛が file: 依存の npm パッケージへ実際に届くことを確認する。",
  "scripts": {
    "typecheck": "tsc --strict --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@kei/runtime": "file:../../../../runtime",
    "@types/node": "^26.1.0",
    "typescript": "^5.5.0",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "greeter": "file:../../../e2e/packages/greeter"
  }
}
