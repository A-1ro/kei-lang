module check.uses_async_contract

record Item {
  id: Int
}

func fetchName(id: Int) -> String
  uses Async
{
  return "user"
}

func greetAll(items: List<Item>) -> List<Item>
  uses Async
  ensures result.all(p => fetchName(p.id) != "")
{
  return items
}
