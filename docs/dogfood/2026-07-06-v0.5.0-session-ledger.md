# Dogfood v0.5.0 — セッション台帳(M32 spread + M33 Map 重点)— 2026-07-06

## 実行条件

これまでと同じヒントモード相当・CLI 検証(plugin 未 enable / MCP 旧版。#101 は継続課題)。Sonnet 初見想定。

## お題

セッション台帳(Map<String, SessionInfo>)の延長操作 2 関数。部分更新は「全項目を書き並べない書き方」を要求(spread 誘導)、反例探索も実施。

## 結果: 9/10 — spread + Map + old(field) を初見一発

- `SessionInfo { ...session, remainingSeconds: ... }`(M32)と `ledger.get` → `match` → `ledger.set`(M33)を修正ループゼロで使用
- `ensures result.remainingSeconds == old(session.remainingSeconds) + extendBySeconds` — old のフィールドアクセス形は SKILL.md に制約が明示されておらず「推測で書いたら通った」(軽微な取説ギャップ、下記)
- `--generative`: ensures が bounded 確認、Map 引数関数が対象外となる挙動も「SKILL.md 記載通り」と事前予測できた(M33 の spec 明記が機能)
- 参照: SKILL.md ×1、examples(inventory.kei / records.kei)×2、check ×2、fmt ×1。spec 本体は開かずに完結

## 取説ギャップ(low)

- `old(expr)` の許容される式形(パラメータのフィールドアクセス可、など)が SKILL.md/spec で明示されていない。今回の推測は成功したが、明文化しておくと安全(v0.6 で spec §4 に 1 行追記候補)

## 総評

9/10。v0.5.0 の主要機能(spread / Map / キャプチャ / 文字列 / contains / &&)がすべてドッグフードで初見実証済みとなった。inventory.kei に実例が集約されている構成が効いている。
