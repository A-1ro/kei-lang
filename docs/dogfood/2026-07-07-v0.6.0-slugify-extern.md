# Dogfood v0.6.0 — slugify extern package(M35/M36 重点)— 2026-07-07

## 実行条件

ヒントモード相当・CLI 検証(plugin 未 enable / MCP 旧版。#101 継続課題)。Sonnet 初見想定。
注記: サブエージェントが途中で誤って旧 MCP kei_spec を 1 回呼ぶ逸脱があった(自己申告・即時軌道修正)。最終成果物は docs/ + CLI のみで作成されており、結果の妥当性への影響は軽微と判断。

## お題

npm `slugify` パッケージの関数を型安全に宣言し、記事タイトル+番号からスラッグ文字列を生成(空タイトル分岐・requires 付き)。宣言漏れ検出モードも試す。

## 結果: 9/10 — extern package を初見一発

```kei
extern package "slugify" as slugify
extern slugify.slugify(text: String) -> String

func articleSlug(title: String, number: Int) -> String
  requires number >= 1
{ ... slugify.slugify(title) + "-" + number.toString() ... }
```

- `kei check` 一発エラーゼロ、fmt 正規形、`--strict-extern` も自力発見して KEI-E3004 warning を確認
- 参照: SKILL.md ×1 のみ(spec・examples 0 回)。「npm パッケージを呼ぶ」節(M36)がお題と 1:1 対応
- 摩擦カテゴリ全ゼロ

## 観測されたギャップ(v0.8 の実需裏付け)

- **default export の extern 宣言方法が SKILL.md から読み取れない**(減点理由)。実務 npm パッケージは default export が多い。→ これは v0.6 ロードマップで意図的に段階2(v0.8 🤝、Hono アダプタと同時設計)へ先送りした項目。実需の初観測として、v0.8 設計時にこのフィードバックを入力にすること

## 総評

9/10。v1.0 blocker 1/3(npm import)がドッグフードで初見実証された。
